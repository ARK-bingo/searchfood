<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.wechating">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@drawable/app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeChating">
        <service
            android:name=".MusicService"
            android:enabled="true"
            android:exported="false"></service>
        <activity
            android:name=".ContactActivity"
            android:exported="false" />
        <activity android:name=".MakeMomentPage" />
        <activity android:name=".music" />
        <activity android:name=".contact" />
        <activity android:name=".EmptyContentPage" />
        <activity android:name=".sensor" />
        <activity android:name=".ChatPage" />
        <activity android:name=".AddFriendPage" />
        <activity
            android:name=".Index"
            android:label="@string/title_activity_index" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistActivity" />
        <receiver android:name=".myreceiver">
            <intent-filter>
                <action android:name="com.example.broadcattest.MY-broadCast" />
            </intent-filter>
        </receiver>
    </application>

</manifest>